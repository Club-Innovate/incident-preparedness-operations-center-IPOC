import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

function assert(condition, message) {
  if (!condition) {
    throw new Error(message);
  }
}

function runUiModernizationSmoke() {
  const scriptDirectory = path.dirname(fileURLToPath(import.meta.url));

  const logisticsCardPath = path.resolve(scriptDirectory, '../src/components/navigation/LogisticsCoordinationCard.tsx');
  const copCardPath = path.resolve(scriptDirectory, '../src/components/cop/CommonOperatingPictureCard.tsx');
  const reportingCardPath = path.resolve(scriptDirectory, '../src/components/layout/ReportingWorkspaceCard.tsx');
  const operationsCardPath = path.resolve(scriptDirectory, '../src/components/navigation/OperationsCoordinationCard.tsx');
  const planningCardPath = path.resolve(scriptDirectory, '../src/components/navigation/PlanningCycleCard.tsx');
  const financeCardPath = path.resolve(scriptDirectory, '../src/components/navigation/FinanceAdministrationCard.tsx');
  const afterActionCardPath = path.resolve(scriptDirectory, '../src/components/navigation/AfterActionReadinessCard.tsx');
  const incidentWorkspaceCardPath = path.resolve(scriptDirectory, '../src/components/layout/IncidentWorkspaceCard.tsx');
  const incidentCommandPaneCardPath = path.resolve(scriptDirectory, '../src/components/incidents/IncidentCommandPaneCard.tsx');
  const executionLaneBoardPath = path.resolve(scriptDirectory, '../src/components/navigation/ExecutionLaneBoard.tsx');
  const executionDependencyUtilsPath = path.resolve(scriptDirectory, '../src/components/navigation/executionDependencyUtils.ts');
  const copHandoffUtilityPath = path.resolve(scriptDirectory, '../src/utils/copHandoffContext.ts');
  const appPath = path.resolve(scriptDirectory, '../src/App.tsx');
  const appShellLayoutPath = path.resolve(scriptDirectory, '../src/components/layout/AppShellLayout.tsx');
  const assistantDockPath = path.resolve(scriptDirectory, '../src/components/agent/AssistantDock.tsx');
  const adminWorkspaceCardPath = path.resolve(scriptDirectory, '../src/components/admin/AdminWorkspaceCard.tsx');
  const apiPath = path.resolve(scriptDirectory, '../src/api.ts');
  const typesPath = path.resolve(scriptDirectory, '../src/types.ts');
  const serverAppSettingsPath = path.resolve(scriptDirectory, '../../IPOC_WEB.Server/appsettings.json');
  const serverAppSettingsDevelopmentPath = path.resolve(scriptDirectory, '../../IPOC_WEB.Server/appsettings.Development.json');
  const serverProgramPath = path.resolve(scriptDirectory, '../../IPOC_WEB.Server/Program.cs');
  const appCssPath = path.resolve(scriptDirectory, '../src/App.css');
  const dashboardSnapshotCardPath = path.resolve(scriptDirectory, '../src/components/layout/DashboardSnapshotCard.tsx');

  const logisticsCardSource = fs.readFileSync(logisticsCardPath, 'utf8');
  const copCardSource = fs.readFileSync(copCardPath, 'utf8');
  const reportingCardSource = fs.readFileSync(reportingCardPath, 'utf8');
  const operationsCardSource = fs.readFileSync(operationsCardPath, 'utf8');
  const planningCardSource = fs.readFileSync(planningCardPath, 'utf8');
  const financeCardSource = fs.readFileSync(financeCardPath, 'utf8');
  const afterActionCardSource = fs.readFileSync(afterActionCardPath, 'utf8');
  const incidentWorkspaceCardSource = fs.readFileSync(incidentWorkspaceCardPath, 'utf8');
  const incidentCommandPaneCardSource = fs.readFileSync(incidentCommandPaneCardPath, 'utf8');
  const executionLaneBoardSource = fs.readFileSync(executionLaneBoardPath, 'utf8');
  const executionDependencyUtilsSource = fs.readFileSync(executionDependencyUtilsPath, 'utf8');
  const copHandoffUtilitySource = fs.readFileSync(copHandoffUtilityPath, 'utf8');
  const appSource = fs.readFileSync(appPath, 'utf8');
  const appShellLayoutSource = fs.readFileSync(appShellLayoutPath, 'utf8');
  const assistantDockSource = fs.readFileSync(assistantDockPath, 'utf8');
  const adminWorkspaceCardSource = fs.readFileSync(adminWorkspaceCardPath, 'utf8');
  const apiSource = fs.readFileSync(apiPath, 'utf8');
  const typesSource = fs.readFileSync(typesPath, 'utf8');
  const serverAppSettingsSource = fs.readFileSync(serverAppSettingsPath, 'utf8');
  const serverAppSettingsDevelopmentSource = fs.readFileSync(serverAppSettingsDevelopmentPath, 'utf8');
  const serverProgramSource = fs.readFileSync(serverProgramPath, 'utf8');
  const appCssSource = fs.readFileSync(appCssPath, 'utf8');
  const dashboardSnapshotCardSource = fs.readFileSync(dashboardSnapshotCardPath, 'utf8');

  assert(logisticsCardSource.includes('<MapContainer'), 'Expected Logistics map container for map-first cockpit UX.');
  assert(logisticsCardSource.includes('Map base layer'), 'Expected map layer switching control in Logistics map command controls.');
  assert(logisticsCardSource.includes('<ScaleControl'), 'Expected scale control support in Logistics map.');
  assert(logisticsCardSource.includes('<ZoomControl'), 'Expected zoom control support in Logistics map.');
  assert(logisticsCardSource.includes('setFitToSignalsNonce'), 'Expected fit-to-signals map control wiring.');
  assert(logisticsCardSource.includes('setFitToKansasNonce'), 'Expected fit-to-extent map control wiring.');
  assert(logisticsCardSource.includes('markerIconForSignal'), 'Expected marker symbol semantic helper for map layer modes.');
  assert(logisticsCardSource.includes('variant={inventoryConstraintVariant}'), 'Expected conditional inventory severity color mapping.');
  assert(logisticsCardSource.includes('variant={facilityConstraintVariant}'), 'Expected conditional shortage severity color mapping.');
  assert(logisticsCardSource.includes('Map marker behavior'), 'Expected clustering toggle control in Logistics map filters.');

  assert(copCardSource.includes('ipoc-cop-cockpit'), 'Expected Common Operating Picture card to adopt production cockpit styling hook.');
  assert(copCardSource.includes('COP regional pressure profile'), 'Expected COP regional pressure chart module.');
  assert(copCardSource.includes('COP constrained locations'), 'Expected COP constrained location surfacing module.');
  assert(copCardSource.includes('AI Incident Co-Pilot'), 'Expected AI Incident Co-Pilot panel in Common Operating Picture.');
  assert(copCardSource.includes('Draft ICS-202 from current operations'), 'Expected ICS-202 draft action in COP AI co-pilot workflow.');
  assert(copCardSource.includes('Draft ICS-203 from current operations'), 'Expected ICS-203 draft action in COP AI co-pilot workflow.');
  assert(copCardSource.includes('Draft ICS-204 from current operations'), 'Expected ICS-204 draft action in COP AI co-pilot workflow.');
  assert(copCardSource.includes('COP_AI_SCOPE'), 'Expected COP AI state persistence scope constant.');
  assert(copCardSource.includes('COP_AI_LOCAL_KEY'), 'Expected COP AI local cache key for persistence.');
  assert(copCardSource.includes('Generate next operational period objectives'), 'Expected COP AI quick prompt for next operational period objectives.');
  assert(copCardSource.includes('Summarize overnight changes'), 'Expected COP AI quick prompt for overnight summary.');
  assert(copCardSource.includes('Recommend resource ordering based on incident complexity'), 'Expected COP AI quick prompt for resource ordering recommendation.');
  assert(copCardSource.includes('Predictive impact analytics'), 'Expected predictive impact analytics panel in COP.');
  assert(copCardSource.includes('Doctrine-aware AI co-pilot draft flow with explicit human approval for COP summaries and ICS product generation.'), 'Expected human-approval guardrail messaging for COP AI co-pilot drafts.');

  assert(reportingCardSource.includes('Report distribution profile (native analytics)'), 'Expected Reports native analytics distribution chart.');
  assert(reportingCardSource.includes('Severity distribution profile'), 'Expected Reports severity visualization profile.');
  assert(reportingCardSource.includes('Power BI is active for enterprise analytics'), 'Expected Reports Power BI/native fallback guidance.');

  assert(operationsCardSource.includes('ipoc-mission-cockpit'), 'Expected Operations card to adopt mission cockpit visual system.');
  assert(planningCardSource.includes('ipoc-mission-cockpit'), 'Expected Planning card to adopt mission cockpit visual system.');
  assert(financeCardSource.includes('ipoc-mission-cockpit'), 'Expected Finance card to adopt mission cockpit visual system.');
  assert(afterActionCardSource.includes('ipoc-mission-cockpit'), 'Expected After Action card to adopt mission cockpit visual system.');

  assert(executionLaneBoardSource.includes('dependency-chain'), 'Expected execution lane dependency-chain sort mode.');
  assert(executionLaneBoardSource.includes('Blocked by dependency'), 'Expected blocked dependency visibility cue in execution lane.');
  assert(executionLaneBoardSource.includes('Resolve blocker assist'), 'Expected resolve blocker assist action in execution lane.');
  assert(executionLaneBoardSource.includes('Due date is required before setting status to In Progress or Completed.'), 'Expected due-date integrity rule for In Progress/Completed transitions.');
  assert(executionLaneBoardSource.includes('Owner is required before setting status to Completed.'), 'Expected owner integrity rule for Completed transitions.');
  assert(executionLaneBoardSource.includes('Complete the blocker directive before setting this directive to Completed.'), 'Expected blocker completion rule before marking dependent directive completed.');

  assert(operationsCardSource.includes('enableDependencySequencing'), 'Expected Operations execution lane to enable dependency sequencing.');
  assert(planningCardSource.includes('enableDependencySequencing'), 'Expected Planning execution lane to enable dependency sequencing.');
  assert(financeCardSource.includes('enableDependencySequencing'), 'Expected Finance execution lane to enable dependency sequencing.');
  assert(afterActionCardSource.includes('enableDependencySequencing'), 'Expected After Action execution lane to enable dependency sequencing.');

  assert(operationsCardSource.includes('Open blockers'), 'Expected Operations playbook card blocker telemetry.');
  assert(planningCardSource.includes('Open blockers'), 'Expected Planning playbook card blocker telemetry.');
  assert(financeCardSource.includes('Open blockers'), 'Expected Finance playbook card blocker telemetry.');
  assert(afterActionCardSource.includes('Open blockers'), 'Expected After Action playbook card blocker telemetry.');

  assert(operationsCardSource.includes('Playbook gates: Surge requires command brief + resource sync; Communications requires cadence confirmation; Dependency focus requires command brief.'), 'Expected Operations module playbook gate messaging.');
  assert(planningCardSource.includes('Playbook gates: P-cycle kickoff requires planning brief; IAP package progression requires objective review; AI summary prep requires SITREP cadence validation and SITREP baseline.'), 'Expected Planning module playbook gate messaging.');
  assert(financeCardSource.includes('Playbook gates: Cost recovery requires cost packet checkpoint; Procurement acceleration requires procurement checkpoint; Forecast prep requires admin follow-up checkpoint.'), 'Expected Finance module playbook gate messaging.');
  assert(afterActionCardSource.includes('Playbook gates: AAR readiness requires closure baseline; corrective closure requires logged corrective actions; retrospective summary requires evidence signals.'), 'Expected After Action module playbook gate messaging.');
  assert(operationsCardSource.includes('Command brief'), 'Expected Operations gate status chip for command brief readiness.');
  assert(planningCardSource.includes('Planning brief'), 'Expected Planning gate status chip for planning brief readiness.');
  assert(financeCardSource.includes('Cost packet'), 'Expected Finance gate status chip for cost packet readiness.');
  assert(afterActionCardSource.includes('Closure baseline'), 'Expected After Action gate status chip for closure readiness baseline.');

  assert(operationsCardSource.includes('handleSurgePlaybookClick'), 'Expected Operations surge playbook guardrail click handler.');
  assert(planningCardSource.includes('handlePcycleKickoffPlaybookClick'), 'Expected Planning P-cycle playbook guardrail click handler.');
  assert(financeCardSource.includes('handleCostRecoveryPlaybookClick'), 'Expected Finance cost-recovery playbook guardrail click handler.');
  assert(afterActionCardSource.includes('handleAarReadinessPlaybookClick'), 'Expected After Action AAR playbook guardrail click handler.');
  assert(operationsCardSource.includes('notifyPlaybookBlocked'), 'Expected Operations blocked playbook toast handler.');
  assert(operationsCardSource.includes('Focused incident selector'), 'Expected Operations workspace to expose in-page focused incident selector label guidance.');
  assert(operationsCardSource.includes('onKeyDown={(event) => {'), 'Expected Operations focused incident selector to support typed key typeahead filtering behavior.');
  assert(operationsCardSource.includes('data-testid="operations-focused-incident-typeahead-summary"'), 'Expected Operations focused incident selector to surface live typeahead status feedback.');
  assert(operationsCardSource.includes('data-testid="operations-focused-incident-typeahead-input"'), 'Expected Operations focused incident selector to expose visible typeahead textbox input.');
  assert(operationsCardSource.includes('data-testid="operations-focused-incident-combobox-options"'), 'Expected Operations focused incident selector combobox list options anchor.');
  assert(operationsCardSource.includes('data-testid="operations-focused-incident-combobox-option"'), 'Expected Operations focused incident selector combobox option anchor.');
  assert(operationsCardSource.includes('Focused incident context'), 'Expected Operations focused incident helper text to be surfaced through info tooltip label.');
  assert(planningCardSource.includes('Focused incident selector'), 'Expected Planning workspace to expose in-page focused incident selector label guidance.');
  assert(planningCardSource.includes('onKeyDown={(event) => {'), 'Expected Planning focused incident selector to support typed key typeahead filtering behavior.');
  assert(planningCardSource.includes('data-testid="planning-focused-incident-typeahead-summary"'), 'Expected Planning focused incident selector to surface live typeahead status feedback.');
  assert(planningCardSource.includes('data-testid="planning-focused-incident-typeahead-input"'), 'Expected Planning focused incident selector to expose visible typeahead textbox input.');
  assert(planningCardSource.includes('data-testid="planning-focused-incident-combobox-options"'), 'Expected Planning focused incident selector combobox list options anchor.');
  assert(planningCardSource.includes('data-testid="planning-focused-incident-combobox-option"'), 'Expected Planning focused incident selector combobox option anchor.');
  assert(planningCardSource.includes('Focused incident context'), 'Expected Planning focused incident helper text to be surfaced through info tooltip label.');
  assert(planningCardSource.includes('notifyPlaybookBlocked'), 'Expected Planning blocked playbook toast handler.');
  assert(financeCardSource.includes('notifyPlaybookBlocked'), 'Expected Finance blocked playbook toast handler.');
  assert(afterActionCardSource.includes('notifyPlaybookBlocked'), 'Expected After Action blocked playbook toast handler.');

  assert(operationsCardSource.includes('onNotify: NotifyHandler'), 'Expected Operations notification callback prop contract.');
  assert(planningCardSource.includes('onNotify: NotifyHandler'), 'Expected Planning notification callback prop contract.');
  assert(financeCardSource.includes('onNotify: NotifyHandler'), 'Expected Finance notification callback prop contract.');
  assert(afterActionCardSource.includes('onNotify: NotifyHandler'), 'Expected After Action notification callback prop contract.');
  assert(incidentWorkspaceCardSource.includes('onNotify: NotifyHandler'), 'Expected Incident workspace notification callback prop contract for handoff continuity alerts.');
  assert(incidentCommandPaneCardSource.includes('Navigate: resolve lifecycle blockers in Resource routing lanes.'), 'Expected Incident resource lifecycle evidence blockers to provide remediation navigation guidance.');
  assert(incidentCommandPaneCardSource.includes('Navigate: resolve IAP governance blockers in Operational Periods.'), 'Expected Incident IAP governance evidence blockers to provide remediation navigation guidance.');
  assert(incidentCommandPaneCardSource.includes('Navigate: add SITREP evidence to clear retrospective blockers.'), 'Expected Incident SITREP evidence remediation navigation guidance.');
  assert(incidentCommandPaneCardSource.includes('Open resource routing lanes to resolve open unassigned requests'), 'Expected Incident resource lifecycle blocker action for open unassigned requests.');
  assert(incidentCommandPaneCardSource.includes('Open Operational Periods to approve a period for IAP governance compliance'), 'Expected Incident governance blocker action for operational period approval.');
  assert(incidentCommandPaneCardSource.includes('Open SITREP tab to build baseline evidence for packet generation'), 'Expected Incident governance blocker action for SITREP evidence generation.');
  assert(incidentCommandPaneCardSource.includes('data-testid="incident-remediation-hint-resources"'), 'Expected Incident resources remediation hint test anchor.');
  assert(incidentCommandPaneCardSource.includes('data-testid="incident-remediation-hint-periods"'), 'Expected Incident periods remediation hint test anchor.');
  assert(incidentCommandPaneCardSource.includes('data-testid="incident-remediation-hint-sitrep"'), 'Expected Incident SITREP remediation hint test anchor.');
  assert(incidentCommandPaneCardSource.includes('testId="incident-remediation-action-resource-open-unassigned"'), 'Expected Incident remediation action test anchor for open unassigned resources.');
  assert(incidentCommandPaneCardSource.includes('testId="incident-remediation-action-resource-transition-coverage"'), 'Expected Incident remediation action test anchor for transition coverage.');
  assert(incidentCommandPaneCardSource.includes('testId="incident-remediation-action-iap-approve-period"'), 'Expected Incident remediation action test anchor for IAP period approval.');
  assert(incidentCommandPaneCardSource.includes('testId="incident-remediation-action-iap-build-packet"'), 'Expected Incident remediation action test anchor for IAP packet build path.');

  assert(copHandoffUtilitySource.includes('COP_COMMAND_HANDOFF_CONTEXT_MAX_AGE_MS = 12 * 60 * 60 * 1000'), 'Expected COP handoff TTL freshness guardrail constant (12h).');
  assert(copHandoffUtilitySource.includes('readCopCommandHandoffContext'), 'Expected shared COP handoff read helper.');
  assert(copHandoffUtilitySource.includes('clearCopCommandHandoffContext'), 'Expected shared COP handoff clear helper.');
  assert(copHandoffUtilitySource.includes('writeCopCommandHandoffContext'), 'Expected shared COP handoff write helper for cross-workspace publication.');
  assert(copHandoffUtilitySource.includes('if (isHandoffExpired(parsed))'), 'Expected stale COP handoff payload expiry check.');
  assert(copHandoffUtilitySource.includes('localStorage.removeItem(COP_COMMAND_HANDOFF_CONTEXT_KEY);'), 'Expected stale COP handoff payload auto-purge behavior.');
  assert(incidentWorkspaceCardSource.includes("readCopCommandHandoffContext('incidents')"), 'Expected Incident workspace to consume shared COP handoff read helper.');
  assert(planningCardSource.includes("readCopCommandHandoffContext('planning')"), 'Expected Planning workspace to consume shared COP handoff read helper.');
  assert(operationsCardSource.includes("readCopCommandHandoffContext('operations')"), 'Expected Operations workspace to consume shared COP handoff read helper.');
  assert(afterActionCardSource.includes("readCopCommandHandoffContext('after-action')"), 'Expected After Action workspace to consume shared COP handoff read helper.');
  assert(incidentWorkspaceCardSource.includes('clearCopHandoffBannerContext('), 'Expected Incident workspace to clear handoff context via shared banner helper.');
  assert(planningCardSource.includes('clearCopHandoffBannerContext('), 'Expected Planning workspace to clear handoff context via shared banner helper.');
  assert(operationsCardSource.includes('clearCopHandoffBannerContext('), 'Expected Operations workspace to clear handoff context via shared banner helper.');
  assert(afterActionCardSource.includes('clearCopHandoffBannerContext('), 'Expected After Action workspace to clear handoff context via shared banner helper.');

  assert(planningCardSource.includes('AI planning summary draft'), 'Expected Planning AI summary draft panel.');
  assert(planningCardSource.includes('generateAiDraftSummary'), 'Expected Planning AI draft generation action.');
  assert(planningCardSource.includes('approveAiDraftSummary'), 'Expected Planning AI draft approval action.');
  assert(planningCardSource.includes('rejectAiDraftSummary'), 'Expected Planning AI draft rejection action.');
  assert(planningCardSource.includes('Human approval is required before AI draft content is used operationally.'), 'Expected explicit human-approval guardrail text for Planning AI draft flow.');

  assert(appSource.includes("const AssistantDock = lazy(() => import('./components/agent/AssistantDock'));"), 'Expected app shell to lazily import AssistantDock.');
  assert(appSource.includes('assistantDock={('), 'Expected app shell to provide assistantDock content to the shell layout.');
  assert(appSource.includes('onNotify={showToast}'), 'Expected app shell to mount AssistantDock with shared notification handler.');
  assert(appSource.includes('isAuthenticated={isAuthenticated}'), 'Expected app shell to pass authentication context into AssistantDock for cloud sync behavior.');
  assert(appShellLayoutSource.includes('assistantDock?: ReactNode;'), 'Expected AppShellLayout assistant dock slot prop contract.');
  assert(appShellLayoutSource.includes('{assistantDock}'), 'Expected AppShellLayout to render assistant dock slot.');
  assert(assistantDockSource.includes('AI Incident Co-Pilot is ready.'), 'Expected AssistantDock seeded assistant welcome message.');
  assert(assistantDockSource.includes('ASSISTANT_LOCAL_STATE_KEY'), 'Expected AssistantDock local persistence key.');
  assert(assistantDockSource.includes('ASSISTANT_SCOPE'), 'Expected AssistantDock backend state preset scope constant.');
  assert(assistantDockSource.includes('ASSISTANT_ANALYTICS_SCOPE'), 'Expected AssistantDock analytics preset scope constant.');
  assert(assistantDockSource.includes('ASSISTANT_AZURE_AI_ENABLED'), 'Expected AssistantDock Azure AI feature flag gate.');
  assert(assistantDockSource.includes('ASSISTANT_AZURE_OPENAI_DEPLOYMENT'), 'Expected AssistantDock Azure OpenAI deployment env binding.');
  assert(assistantDockSource.includes('ASSISTANT_AZURE_OPENAI_API_VERSION'), 'Expected AssistantDock Azure OpenAI API version env binding.');
  assert(assistantDockSource.includes('Configuration required'), 'Expected AssistantDock connection status for missing Azure configuration.');
  assert(assistantDockSource.includes('getAgentConfigHealth()'), 'Expected AssistantDock backend health diagnostics call for Azure config readiness.');
  assert(assistantDockSource.includes('Service unavailable'), 'Expected AssistantDock connection status when backend Azure diagnostics are unavailable.');
  assert(assistantDockSource.includes('upsertUserReportPreset(ASSISTANT_SCOPE'), 'Expected AssistantDock cloud sync write for conversation state.');
  assert(assistantDockSource.includes('upsertUserReportPreset(ASSISTANT_ANALYTICS_SCOPE'), 'Expected AssistantDock cloud sync write for analytics state.');
  assert(assistantDockSource.includes('completeAgentChat({'), 'Expected AssistantDock Azure-grounded chat completion call when feature flag is enabled.');
  assert(assistantDockSource.includes('getAgentConversationHistory()'), 'Expected AssistantDock Azure-enabled conversation history retrieval hook.');
  assert(assistantDockSource.includes('createAgentAnalyticsEvent({'), 'Expected AssistantDock Azure-enabled analytics event hook.');
  assert(assistantDockSource.includes('Confidence'), 'Expected AssistantDock trust UX confidence indicator for assistant responses.');
  assert(assistantDockSource.includes('Citations:'), 'Expected AssistantDock trust UX citations indicator for assistant responses.');
  assert(assistantDockSource.includes('Fallback'), 'Expected AssistantDock fallback badge for non-grounded responses.');
  assert(assistantDockSource.includes('setMessageApprovalStatus'), 'Expected AssistantDock human approval checkpoint handler for assistant recommendations.');
  assert(assistantDockSource.includes('Approve'), 'Expected AssistantDock approve action button for pending assistant recommendations.');
  assert(assistantDockSource.includes('Reject'), 'Expected AssistantDock reject action button for pending assistant recommendations.');
  assert(assistantDockSource.includes('renameActiveSession'), 'Expected AssistantDock rename action for active conversation.');
  assert(assistantDockSource.includes('sessionRenamedCount'), 'Expected AssistantDock analytics counter for renamed sessions.');
  assert(assistantDockSource.includes('Conversations'), 'Expected AssistantDock conversation session rail.');
  assert(assistantDockSource.includes('Personalization'), 'Expected AssistantDock personalization controls.');

  assert(reportingCardSource.includes('Comparative regional/facility lens'), 'Expected Reports comparative drill-through lens card for executive analytics.');
  assert(reportingCardSource.includes('data-testid="reports-comparison-left-select"'), 'Expected Reports comparative lens left selector test anchor.');
  assert(reportingCardSource.includes('data-testid="reports-comparison-right-select"'), 'Expected Reports comparative lens right selector test anchor.');
  assert(reportingCardSource.includes('data-testid="reports-comparison-narrative"'), 'Expected Reports comparative lens narrative test anchor.');
  assert(reportingCardSource.includes('data-testid="reports-comparison-metrics-table"'), 'Expected Reports comparative lens metrics table test anchor.');
  assert(reportingCardSource.includes('testId="reports-comparison-apply-left"'), 'Expected Reports comparative lens action for left-group drill-through.');
  assert(reportingCardSource.includes('testId="reports-comparison-apply-left-risk"'), 'Expected Reports comparative lens action for left-group high-severity drill-through.');
  assert(reportingCardSource.includes('testId="reports-comparison-apply-right"'), 'Expected Reports comparative lens action for right-group drill-through.');
  assert(reportingCardSource.includes('testId="reports-comparison-apply-right-risk"'), 'Expected Reports comparative lens action for right-group high-severity drill-through.');
  assert(reportingCardSource.includes('testId="reports-comparison-swap"'), 'Expected Reports comparative lens action for swapping left/right sides.');
  assert(reportingCardSource.includes('testId="reports-comparison-auto-top2"'), 'Expected Reports comparative lens action for auto-picking top two groups.');
  assert(reportingCardSource.includes('data-testid="reports-executive-decision-queue-card"'), 'Expected Reports executive decision queue card.');
  assert(reportingCardSource.includes('data-testid="reports-executive-decision-queue-table"'), 'Expected Reports executive decision queue table anchor.');
  assert(reportingCardSource.includes('data-testid="reports-risk-timeline-card"'), 'Expected Reports risk timeline card.');
  assert(reportingCardSource.includes('data-testid="reports-risk-timeline-chart"'), 'Expected Reports risk timeline chart anchor.');
  assert(reportingCardSource.includes('testId="reports-decision-queue-handoff-incidents"'), 'Expected Reports decision queue handoff action to Incident workspace.');
  assert(reportingCardSource.includes('data-testid="reports-decision-queue-stage-top3"'), 'Expected Reports decision queue stage-top3 action anchor.');
  assert(reportingCardSource.includes('onClick={stageTopDecisionQueueForPendingApproval}'), 'Expected Reports decision queue stage-top3 action to call queue staging handler.');
  assert(reportingCardSource.includes('testId="reports-decision-queue-stage-pending"'), 'Expected Reports decision queue per-row stage-to-pending action anchor.');
  assert(reportingCardSource.includes('onClick={() => stageDecisionQueueRowForPendingApproval(row)}'), 'Expected Reports decision queue per-row stage action to call row staging handler.');
  assert(reportingCardSource.includes('stageDecisionQueueRowForPendingApproval'), 'Expected Reports queue-to-pending staging handler wiring.');
  assert(reportingCardSource.includes('stageTopDecisionQueueForPendingApproval'), 'Expected Reports queue-to-pending stage-top handler wiring.');
  assert(reportingCardSource.includes('testId="reports-decision-queue-handoff-operations"'), 'Expected Reports decision queue handoff action to Operations workspace.');
  assert(reportingCardSource.includes('testId="reports-decision-queue-handoff-planning"'), 'Expected Reports decision queue handoff action to Planning workspace.');
  assert(reportingCardSource.includes('testId="reports-decision-queue-handoff-after-action"'), 'Expected Reports decision queue handoff action to After Action workspace.');
  assert(reportingCardSource.includes('publishDecisionQueueHandoffAndNavigate'), 'Expected Reports decision queue handoff actions to support immediate workspace navigation.');
  assert(reportingCardSource.includes('Pending approvals with confidence recommendations'), 'Expected Reports pending approvals module with confidence-labeled recommendations.');
  assert(reportingCardSource.includes('<LabelWithInfo text="Preset name"'), 'Expected Reports preset-name label to include LabelWithInfo tooltip guidance.');
  assert(reportingCardSource.includes('<LabelWithInfo text="Compare left"'), 'Expected Reports compare-left label to include LabelWithInfo tooltip guidance.');
  assert(reportingCardSource.includes('<LabelWithInfo text="Trend window"'), 'Expected Reports trend-window label to include LabelWithInfo tooltip guidance.');
  assert(reportingCardSource.includes('data-testid="reports-pending-approvals-card"'), 'Expected Reports pending approvals card test anchor.');
  assert(reportingCardSource.includes('data-testid="reports-pending-approvals-table"'), 'Expected Reports pending approvals table test anchor.');
  assert(reportingCardSource.includes('testId="reports-pending-approvals-apply"'), 'Expected Reports pending approvals apply action test anchor.');
 assert(reportingCardSource.includes('testId="reports-pending-approvals-approve"'), 'Expected Reports pending approvals approve action test anchor.');
 assert(reportingCardSource.includes('testId="reports-pending-approvals-defer"'), 'Expected Reports pending approvals defer action test anchor.');
 assert(reportingCardSource.includes('testId="reports-pending-approvals-reject"'), 'Expected Reports pending approvals reject action test anchor.');
 assert(reportingCardSource.includes('REPORT_APPROVAL_DECISIONS_SCOPE'), 'Expected Reports pending approvals decision persistence scope constant.');
  assert(reportingCardSource.includes('data-testid="reports-pending-approvals-rationale-input"'), 'Expected Reports pending approvals rationale input anchor.');
  assert(reportingCardSource.includes('data-testid="reports-pending-approvals-rationale-text"'), 'Expected Reports pending approvals rationale display anchor.');
  assert(reportingCardSource.includes('testId="reports-pending-approvals-export-summary"'), 'Expected Reports pending approvals executive summary export action anchor.');
  assert(reportingCardSource.includes('data-testid="reports-pending-approvals-export-mode"'), 'Expected Reports pending approvals export mode selector anchor.');
  assert(reportingCardSource.includes('data-testid="reports-pending-approvals-export-empty-rationale"'), 'Expected Reports pending approvals export toggle for empty rationale rows.');
  assert(reportingCardSource.includes('Include empty rationale'), 'Expected Reports pending approvals export empty rationale label.');
  assert(reportingCardSource.includes('data-testid="reports-pending-approvals-confidence-floor"'), 'Expected Reports pending approvals confidence floor selector anchor.');
  assert(reportingCardSource.includes('data-testid="reports-pending-approvals-batch-toolbar"'), 'Expected Reports pending approvals batch toolbar anchor.');
  assert(reportingCardSource.includes('testId="reports-pending-approvals-select-all"'), 'Expected Reports pending approvals select-all action anchor.');
  assert(reportingCardSource.includes('testId="reports-pending-approvals-clear-all"'), 'Expected Reports pending approvals clear-all action anchor.');
  assert(reportingCardSource.includes('onClick={selectAllPendingApprovals}'), 'Expected Reports pending approvals select-all action wiring.');
  assert(reportingCardSource.includes('onClick={clearAllPendingApprovals}'), 'Expected Reports pending approvals clear-all action wiring.');
  assert(reportingCardSource.includes('data-testid="reports-pending-approvals-row-select"'), 'Expected Reports pending approvals row selection anchor.');
  assert(reportingCardSource.includes('testId="reports-pending-approvals-batch-approve"'), 'Expected Reports pending approvals batch approve action anchor.');
  assert(reportingCardSource.includes('testId="reports-pending-approvals-batch-defer"'), 'Expected Reports pending approvals batch defer action anchor.');
  assert(reportingCardSource.includes('testId="reports-pending-approvals-batch-reject"'), 'Expected Reports pending approvals batch reject action anchor.');
  assert(reportingCardSource.includes('data-testid="reports-pending-approvals-batch-actions-row"'), 'Expected Reports pending approvals actions to render in dedicated one-row icon action strip.');
  assert(reportingCardSource.includes('role="toolbar"'), 'Expected Reports pending approvals actions strip to render toolbar semantics.');
  assert(reportingCardSource.includes('className="reports-batch-action-select"'), 'Expected Reports pending approvals select-all icon action style hook.');
  assert(reportingCardSource.includes('className="reports-batch-action-clear"'), 'Expected Reports pending approvals clear icon action style hook.');
  assert(reportingCardSource.includes('className="reports-batch-action-approve"'), 'Expected Reports pending approvals approve icon action style hook.');
  assert(reportingCardSource.includes('className="reports-batch-action-defer"'), 'Expected Reports pending approvals defer icon action style hook.');
  assert(reportingCardSource.includes('className="reports-batch-action-reject"'), 'Expected Reports pending approvals reject icon action style hook.');
  assert(reportingCardSource.includes('iconClassName="bi bi-x-circle"'), 'Expected Reports pending approvals clear action to use icon button affordance.');
  assert(reportingCardSource.includes('iconClassName="bi bi-check2-circle"'), 'Expected Reports pending approvals batch approve action to use icon button affordance.');
  assert(reportingCardSource.includes('iconClassName="bi bi-clock-history"'), 'Expected Reports pending approvals batch defer action to use icon button affordance.');
  assert(reportingCardSource.includes('iconClassName="bi bi-x-octagon"'), 'Expected Reports pending approvals batch reject action to use icon button affordance.');
  assert(reportingCardSource.includes('variant="outline-primary"'), 'Expected Reports pending approvals defer action styling to avoid yellow warning emphasis.');
  assert(reportingCardSource.includes('applyBatchPendingApprovalDecision(\'Approved\')'), 'Expected Reports pending approvals batch approve wiring to Approved decision state.');
  assert(reportingCardSource.includes('applyBatchPendingApprovalDecision(\'Deferred\')'), 'Expected Reports pending approvals batch defer wiring to Deferred decision state.');
  assert(reportingCardSource.includes('applyBatchPendingApprovalDecision(\'Rejected\')'), 'Expected Reports pending approvals batch reject wiring to Rejected decision state.');
  assert(reportingCardSource.includes('applyBatchPendingApprovalDecision'), 'Expected Reports pending approvals batch decision handler wiring.');
  assert(reportingCardSource.includes('testId="reports-template-preset-apply"'), 'Expected Reports template preset apply action icon-button anchor.');
  assert(reportingCardSource.includes('testId="reports-template-preset-rename"'), 'Expected Reports template preset rename action icon-button anchor.');
  assert(reportingCardSource.includes('testId="reports-template-preset-delete"'), 'Expected Reports template preset delete action icon-button anchor.');
  assert(reportingCardSource.includes('testId="reports-clear-drilldown"'), 'Expected Reports clear drill-down icon-button action anchor.');
  assert(reportingCardSource.includes('data-testid="reports-pending-approvals-history-card"'), 'Expected Reports pending approvals decision history replay card anchor.');
  assert(reportingCardSource.includes('data-testid="reports-pending-approvals-history-list"'), 'Expected Reports pending approvals decision history list anchor.');
  assert(reportingCardSource.includes('testId="reports-pending-approvals-history-replay"'), 'Expected Reports pending approvals decision history replay action anchor.');
  assert(reportingCardSource.includes('Decision replay context applied for ${entry.incidentNumber}.'), 'Expected Reports pending approvals history replay success notification wiring.');
  assert(reportingCardSource.includes('Replay context is outside the current report filter scope.'), 'Expected Reports pending approvals history replay out-of-scope guardrail wiring.');
  assert(reportingCardSource.includes('REPORT_DECISION_HISTORY_SCOPE'), 'Expected Reports pending approvals decision history persistence scope constant.');
  assert(reportingCardSource.includes('reports-pending-approval-executive-summary'), 'Expected Reports pending approvals executive summary CSV filename prefix wiring.');
  assert(reportingCardSource.includes('Export all recommendations'), 'Expected Reports pending approvals export mode option for all recommendations.');
  assert(reportingCardSource.includes('GeneratedUtc'), 'Expected Reports pending approvals CSV metadata header generation.');
  assert(reportingCardSource.includes('IncludeEmptyRationale'), 'Expected Reports pending approvals CSV metadata to include empty-rationale toggle state.');
  assert(reportingCardSource.includes('data-testid="reports-pending-approvals-trend-card"'), 'Expected Reports pending approvals decision trend card anchor.');
  assert(reportingCardSource.includes('data-testid="reports-pending-approvals-trend-chart"'), 'Expected Reports pending approvals decision trend chart anchor.');
  assert(reportingCardSource.includes('Pending approval decision trend'), 'Expected Reports pending approvals decision trend heading.');
  assert(reportingCardSource.includes('resolveRecommendation'), 'Expected Reports recommendation resolver for confidence-labeled predictive actions.');
  assert(appSource.includes('onNavigateToView={(view) => handleNavigate(view)}'), 'Expected app shell to wire reporting workspace handoff navigation callback.');
  assert(appSource.includes('onNotify={showToast}'), 'Expected app shell to wire reporting workspace notifications for recommendation/handoff feedback.');
  assert(copCardSource.includes('<LabelWithInfo text="AOI Region"'), 'Expected COP AOI Region label to include LabelWithInfo guidance tooltip.');
  assert(copCardSource.includes('<LabelWithInfo text="Layer set name"'), 'Expected COP layer set name label to include LabelWithInfo guidance tooltip.');
  assert(logisticsCardSource.includes('<LabelWithInfo text="Map signal layer"'), 'Expected Logistics map signal layer label to include LabelWithInfo guidance tooltip.');
  assert(logisticsCardSource.includes('<LabelWithInfo text="Inventory ranking mode"'), 'Expected Logistics inventory ranking label to include LabelWithInfo guidance tooltip.');
  assert(operationsCardSource.includes('<LabelWithInfo text="Operations command mode"'), 'Expected Operations command mode label to include LabelWithInfo guidance tooltip.');
  assert(planningCardSource.includes('<LabelWithInfo text="Planning cadence mode"'), 'Expected Planning cadence label to include LabelWithInfo guidance tooltip.');
  assert(financeCardSource.includes('<LabelWithInfo text="Finance command mode"'), 'Expected Finance command mode label to include LabelWithInfo guidance tooltip.');
  assert(financeCardSource.includes('title="Finance execution lane"'), 'Expected Finance execution lane board title anchor.');
  assert(financeCardSource.includes('titleInfo="Command lane for sequencing FEMA readiness, procurement orchestration, governance checkpoints, and predictive pressure work in one dependency-aware board."'), 'Expected Finance execution lane board to include descriptive tooltip guidance.');
  assert(financeCardSource.includes("capability: 'FEMA reimbursement readiness lane'"), 'Expected Finance FEMA reimbursement readiness lane capability anchor.');
  assert(financeCardSource.includes("capabilityInfo: 'Tracks reimbursement packet readiness, supporting documentation traceability, and export-grade evidence posture for FEMA-aligned submissions.'"), 'Expected Finance FEMA reimbursement readiness lane capability tooltip guidance.');
  assert(financeCardSource.includes("capability: 'Procurement packet orchestration'"), 'Expected Finance procurement packet orchestration capability anchor.');
  assert(financeCardSource.includes("capabilityInfo: 'Coordinates high-priority procurement packets, supplier documentation flow, and request-to-contract alignment for focused incident demand.'"), 'Expected Finance procurement packet orchestration capability tooltip guidance.');
  assert(financeCardSource.includes("capability: 'Admin governance and audit checkpoints'"), 'Expected Finance admin governance and audit checkpoints capability anchor.');
  assert(financeCardSource.includes("capabilityInfo: 'Monitors finance and administrative control gates so approvals, checklist progression, and audit evidence remain defensible and complete.'"), 'Expected Finance admin governance and audit checkpoints capability tooltip guidance.');
  assert(financeCardSource.includes('text="Finance/Admin checkpoint actions"'), 'Expected Finance checkpoint actions header to include LabelWithInfo guidance tooltip.');
  assert(reportingCardSource.includes('<LabelWithInfo text="Favorite template name"'), 'Expected Reports favorite template name label to include LabelWithInfo guidance tooltip.');
  assert(reportingCardSource.includes('<LabelWithInfo text="Agent visualization prompt"'), 'Expected Reports agent visualization prompt label to include LabelWithInfo guidance tooltip.');
  assert(incidentCommandPaneCardSource.includes('<LabelWithInfo text="Operational Period"'), 'Expected Incident SITREP operational period label to include LabelWithInfo guidance tooltip.');
  assert(incidentCommandPaneCardSource.includes('<LabelWithInfo text="Summary"'), 'Expected Incident SITREP summary label to include LabelWithInfo guidance tooltip.');
  assert(incidentCommandPaneCardSource.includes('<LabelWithInfo text="Current Actions"'), 'Expected Incident SITREP current actions label to include LabelWithInfo guidance tooltip.');
  assert(incidentCommandPaneCardSource.includes('<LabelWithInfo text="Planned Actions"'), 'Expected Incident SITREP planned actions label to include LabelWithInfo guidance tooltip.');
  assert(incidentCommandPaneCardSource.includes('<LabelWithInfo text="Unmet Needs"'), 'Expected Incident SITREP unmet needs label to include LabelWithInfo guidance tooltip.');
  assert(adminWorkspaceCardSource.includes('<LabelWithInfo text="Display name"'), 'Expected Admin user create display name label to include LabelWithInfo guidance tooltip.');
  assert(adminWorkspaceCardSource.includes('<LabelWithInfo text="Email"'), 'Expected Admin user create email label to include LabelWithInfo guidance tooltip.');
  assert(adminWorkspaceCardSource.includes('<LabelWithInfo text="UPN"'), 'Expected Admin user create UPN label to include LabelWithInfo guidance tooltip.');
  assert(adminWorkspaceCardSource.includes('<LabelWithInfo text="Initial status"'), 'Expected Admin user create initial status label to include LabelWithInfo guidance tooltip.');
  assert(adminWorkspaceCardSource.includes('<LabelWithInfo text="Entra Object ID"'), 'Expected Admin user create Entra Object ID label to include LabelWithInfo guidance tooltip.');
  assert(adminWorkspaceCardSource.includes('Bulk user import (CSV)'), 'Expected Admin workspace to include bulk user import section.');
  assert(adminWorkspaceCardSource.includes('data-testid="admin-user-bulk-import-file-input"'), 'Expected Admin bulk user import file input test anchor.');
  assert(adminWorkspaceCardSource.includes('testId="admin-user-bulk-import-run"'), 'Expected Admin bulk user import action anchor.');
  assert(adminWorkspaceCardSource.includes('data-testid="admin-user-bulk-import-summary"'), 'Expected Admin bulk user import summary anchor.');
  assert(adminWorkspaceCardSource.includes('<LabelWithInfo text="Assignment reason"'), 'Expected Admin role assignment reason label to include LabelWithInfo guidance tooltip.');
  assert(adminWorkspaceCardSource.includes('<LabelWithInfo text="Latitude"'), 'Expected Admin geolocation latitude label to include LabelWithInfo guidance tooltip.');
  assert(adminWorkspaceCardSource.includes('<LabelWithInfo text="Longitude"'), 'Expected Admin geolocation longitude label to include LabelWithInfo guidance tooltip.');
  assert(adminWorkspaceCardSource.includes('<LabelWithInfo text="City"'), 'Expected Admin geolocation city label to include LabelWithInfo guidance tooltip.');
  assert(adminWorkspaceCardSource.includes('<LabelWithInfo text="State"'), 'Expected Admin geolocation state label to include LabelWithInfo guidance tooltip.');
  assert(adminWorkspaceCardSource.includes('<LabelWithInfo text="Postal Code"'), 'Expected Admin geolocation postal code label to include LabelWithInfo guidance tooltip.');
  assert(adminWorkspaceCardSource.includes('<LabelWithInfo text="Confirmation acknowledgment"'), 'Expected Admin synthetic data confirmation label to include LabelWithInfo guidance tooltip.');
  assert(afterActionCardSource.includes('<LabelWithInfo text="After Action command mode"'), 'Expected After Action command mode label to include LabelWithInfo guidance tooltip.');
  assert(assistantDockSource.includes('<LabelWithInfo text="Bot bubble"'), 'Expected Assistant personalization color labels to include LabelWithInfo guidance tooltip.');
  assert(dashboardSnapshotCardSource.includes('<LabelWithInfo text="Primary"'), 'Expected Dashboard palette primary color label to include LabelWithInfo guidance tooltip.');
  assert(dashboardSnapshotCardSource.includes('<LabelWithInfo text="Favorite template name"'), 'Expected Dashboard favorite template name label to include LabelWithInfo guidance tooltip.');
  assert(dashboardSnapshotCardSource.includes('<LabelWithInfo text="Agent visualization prompt"'), 'Expected Dashboard agent visualization prompt label to include LabelWithInfo guidance tooltip.');
  assert(typesSource.includes('incidentId?: number | null;'), 'Expected shared COP handoff contract to include incidentId context.');
  assert(typesSource.includes('incidentNumber?: string | null;'), 'Expected shared COP handoff contract to include incidentNumber context.');
  assert(typesSource.includes('incidentName?: string | null;'), 'Expected shared COP handoff contract to include incidentName context.');

  assert(apiSource.includes('getAgentConversationHistory'), 'Expected backend-ready API client method for loading agent conversation history.');
  assert(apiSource.includes('completeAgentChat'), 'Expected backend-ready API client method for agent chat completion.');
  assert(apiSource.includes('saveAgentPersonalization'), 'Expected backend-ready API client method for agent personalization persistence.');
  assert(apiSource.includes('createAgentAnalyticsEvent'), 'Expected backend-ready API client method for agent analytics event ingestion.');
  assert(apiSource.includes('getAgentConfigHealth'), 'Expected frontend API client method for agent config health endpoint.');

  assert(typesSource.includes('export interface AgentChatCompletionRequest'), 'Expected agent chat completion request contract in shared types.');
  assert(typesSource.includes('export interface AgentChatCompletionResponse'), 'Expected agent chat completion response contract in shared types.');
  assert(typesSource.includes('export interface AgentConversationSession'), 'Expected agent conversation session contract in shared types.');
  assert(typesSource.includes('export interface AgentAnalyticsEventRequest'), 'Expected agent analytics event contract in shared types.');

  assert(serverAppSettingsSource.includes('"AzureOpenAI"'), 'Expected server appsettings baseline to include AzureOpenAI section.');
  assert(serverAppSettingsSource.includes('"AzureAISearch"'), 'Expected server appsettings baseline to include AzureAISearch section.');
  assert(serverAppSettingsDevelopmentSource.includes('"AzureOpenAI"'), 'Expected server development appsettings to include AzureOpenAI overrides section.');
  assert(serverAppSettingsDevelopmentSource.includes('"AzureAISearch"'), 'Expected server development appsettings to include AzureAISearch overrides section.');
  assert(serverProgramSource.includes("var agent = apiV1.MapGroup(\"/agent\")"), 'Expected server to define /api/v1/agent route group.');
  assert(serverProgramSource.includes('agent.MapGet("/history"'), 'Expected server to expose agent conversation history endpoint.');
  assert(serverProgramSource.includes('agent.MapPost("/chat/completions"'), 'Expected server to expose agent chat completion endpoint.');
  assert(serverProgramSource.includes('agent.MapPost("/personalization"'), 'Expected server to expose agent personalization endpoint.');
  assert(serverProgramSource.includes('agent.MapPost("/analytics/events"'), 'Expected server to expose agent analytics events endpoint.');

  assert(executionDependencyUtilsSource.includes('applyDirectivePatch('), 'Expected shared directive patch helper in dependency utils.');
  assert(executionDependencyUtilsSource.includes('applyDirectivePatchBatch('), 'Expected shared directive batch patch helper in dependency utils.');
  assert(executionDependencyUtilsSource.includes('countUnresolvedDependencies('), 'Expected unresolved dependency helper in dependency utils.');
  assert(executionDependencyUtilsSource.includes('resolveDependencyBlockers('), 'Expected resolve blocker helper in dependency utils.');

  assert(appCssSource.includes('.ipoc-mission-cockpit'), 'Expected shared mission cockpit CSS system.');
  assert(appCssSource.includes('.ipoc-operations-cockpit'), 'Expected module-specific Operations accent CSS.');
  assert(appCssSource.includes('.ipoc-planning-cockpit'), 'Expected module-specific Planning accent CSS.');
  assert(appCssSource.includes('.ipoc-finance-cockpit'), 'Expected module-specific Finance accent CSS.');
  assert(appCssSource.includes('.ipoc-after-action-cockpit'), 'Expected module-specific After Action accent CSS.');
  assert(appCssSource.includes('.ipoc-mission-kpi-card'), 'Expected shared mission KPI card CSS for production analytics modules.');
  assert(appCssSource.includes('.ipoc-agent-launcher-btn'), 'Expected floating agent launcher styling in app shell CSS.');
  assert(appCssSource.includes('.ipoc-agent-dock'), 'Expected docked agent pane styling in app shell CSS.');
  assert(appCssSource.includes('.ipoc-agent-message-stream'), 'Expected assistant message stream styling in app shell CSS.');
  assert(appCssSource.includes('.leaflet-container'), 'Expected Leaflet base style integration.');
}

runUiModernizationSmoke();
console.log('[PASS ] Frontend UI modernization smoke checks passed.');
